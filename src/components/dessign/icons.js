import React from 'react';
import { Icon } from 'antd'

const Add = ()=> (
    <Icon type="plus" />
);

const Edit = () => ( 
    <Icon type="edit" />
);

const Cancel = () => (
    <Icon type="cross" />
);

const Save = () => (
    <Icon type="save" />
);

const Delete = () => (
    <Icon type="delete" />
);

export { Add, Edit, Save, Delete, Cancel }